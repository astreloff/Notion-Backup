# Константы

**Константа** — способ сохранить информацию и дать ей имя для последующего использования в коде.

Константы нельзя изменять, в отличие от переменных.

Названия констант пишутся в верхнем регистре и разделяются знаком `_`. Например: `CUSTOM_NUMBER_SETTING`.