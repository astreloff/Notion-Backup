# Переменные

**Переменная** — способ сохранить информацию и дать ей имя для последующего использования в коде.

Названия переменных должны передавать смысл чисел, но должны при этом оставаться достаточно короткими и ёмкими для комфортного чтения.

<aside>
ℹ️ **Как правильно называть переменные?** [Ссылка 1](https://ru.hexlet.io/blog/posts/naming-in-programming), [Ссылка 2](https://ru.hexlet.io/blog/posts/naming-errors-1)

</aside>

Переменные в Python не требуют специального объявления. Вместо этого переменная объявляется при первом её использовании в программе.

<aside>
👍 Переменные принято создавать как можно ближе к тому месту где они используются, для удобства анализа программы.

</aside>

- Переменная должна быть определена до того, как будет использована
- Переменная может содержать как значение, так и выражение. Пример: `number = 2` и `expression = 2*2`.
- Переменная чувствительна к регистру. Пример: `x` и `X` — разные переменные.
- Переменная может содержать в названии только буквы английского алфавита, цифры и знак `_`.